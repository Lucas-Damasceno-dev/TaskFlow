spring:

  application:
    name: "TaskFlow - Project Management System (Backend)"

  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  logging:
    level:
      root: WARN
        #com.lucasdamasceno.taskflow: DEBUG
